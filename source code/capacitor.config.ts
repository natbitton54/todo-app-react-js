import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.natbitton.taskmanager',
  appName: 'Task Manager',
  webDir: 'build'
};

export default config;
